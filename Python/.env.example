# Unreal MCP Server (.env)
#
# Learning mode is intended for inspection/debug + human guidance.
# When enabled, write/automation tools are blocked (compile is allowed).

UNREAL_MCP_LEARNING_MODE=true
